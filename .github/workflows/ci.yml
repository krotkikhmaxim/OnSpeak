name: CI — Lint & Test
on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: speakup
          POSTGRES_PASSWORD: speakup
          POSTGRES_DB: speakup
        ports: ["5432:5432"]
        options: --health-cmd "pg_isready -U speakup" --health-interval 10s --health-timeout 5s --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.10"}
      - name: Install deps
        run: pip install -r services/matching/requirements.txt pytest httpx
      - name: Apply DB schema
        env: {PGPASSWORD: speakup}
        run: psql -h localhost -U speakup -d speakup -f services/matching/init.sql
      - name: Lint
        run: pip install ruff && ruff check services/matching/ tests/ --ignore E501
      - name: Test
        env:
          DATABASE_URL: postgresql://speakup:speakup@localhost:5432/speakup
          REDIS_URL: redis://localhost:6379
          MLFLOW_TRACKING_URI: http://localhost:5000
        run: pytest tests/ -v
